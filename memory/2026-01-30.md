## 2026-01-30
- Myroslav wants proof that inbound work emails are processed and drafts created.
- Requested adding Gmail labels after classifying emails as personal vs mass mail.
- Labels to use: `personal` and `mass` (confirmed 'Так').
- If labels do not exist, create them (can be done via gog/Gmail API).
